# Core (numpy<2 avoids incompatibility with pyarrow/numexpr/bottleneck in many environments)
numpy>=1.20.0,<2
pandas>=2.0.0,<3.0.0

# Web UI for Stage 3 review
streamlit>=1.40.0,<2.0.0

# Dev
pytest>=7.0.0,<9.0.0
