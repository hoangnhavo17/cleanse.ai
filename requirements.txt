# Core (numpy<2 avoids incompatibility with many compiled deps)
numpy>=1.20.0,<2
pandas>=2.0.0,<3.0.0

# HTTP client (used by Gemini API calls)
requests>=2.28.0,<3.0.0

# Web UI for Cleanse.ai review
streamlit>=1.40.0,<2.0.0

# Dev
pytest>=7.0.0,<9.0.0
